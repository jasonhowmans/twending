<ul id="trends-list">
	<% @trends.each do |trend| %>
		<li><h2><%= trend[:name] %></h2>
			<ul class="articles-list" data-trend="<%= trend[:name].gsub('#','') %>">
				<li class="im_loading">Loading stories</li>
			</ul></li>
	<% end %>
</ul>